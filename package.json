{
  "name": "filtered",
  "version": "0.0.1",
  "description": "Text based filter and search for firebase firestore and array of objects",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "firebase",
    "firestore",
    "filter",
    "search",
    "query"
  ],
  "author": "Maye Edwin",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^14.6.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.9",
    "prettier": "^2.0.5",
    "ts-jest": "^26.2.0",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.9.7"
  }
}
